---
# Ansible inventory for DBMCP development environment
all:
  children:
    dbmcp:
      hosts:
        dbmcp-development:
          ansible_host: 3.233.240.153
          ansible_user: ec2-user
          ansible_ssh_private_key_file: /home/andrew/Work/leaninnovationlabs/dbmcp/.ssh/dbmcp-development.pem
          app_environment: development
          
          # Application configuration
          fastapi_port: 8000
          mcp_port: 4200
          
          # AWS configuration
          aws_region: us-east-1
          ecr_registry: "767397957512.dkr.ecr.us-east-1.amazonaws.com"
          
          # Container images
          fastapi_image: "{{ ecr_registry }}/dbmcp-fastapi:latest"
          mcp_image: "{{ ecr_registry }}/dbmcp-mcp:latest"
          
  vars:
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
    ansible_python_interpreter: /usr/bin/python3