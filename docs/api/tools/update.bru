meta {
  name: update
  type: http
  seq: 4
}

put {
  url: {{server}}/tools/1
  body: json
  auth: none
}

body:json {
  {
      "name": "get_trans",
      "description": "Get all the transactions",
      "sql": "SELECT * FROM transactions ",
      "datasource_id": 2,
      "parameters": [
        {
          "name": "class",
          "type": "string",
          "description": "Classification",
          "required": false
        }
      ]
    }
}
