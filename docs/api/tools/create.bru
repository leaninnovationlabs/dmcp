meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{server}}/tools
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "name": "process_user_data",
    "description": "Fetch user data and transform it",
    "type": "code",
    "tool_code": "# Query the database\holidays = await execute_sql(\"lildata\", \"select * from lildata.holidays\", {})\n\nnresult=holidays",
    "datasource_id": 1,
    "parameters": []
  }
  
}
