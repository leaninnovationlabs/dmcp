meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{server}}/tools
  body: json
  auth: none
}

body:json {
  {
      "name": "aws_cost_by_service",
      "description": "Gives you the cost by region",
      "sql": "SELECT line_item_product_code, SUM(line_item_unblended_cost) as total_cost FROM aws_cost_usage_report  GROUP BY line_item_product_code  ORDER BY total_cost DESC;",
      "datasource_id": 1,
      "parameters": []
    }
}
